import React from 'react'
import { shallow } from 'enzyme'
import {name} from './'

describe('{name} container', () => {
  const props = {

  }

  const item = shallow(<{name} {...props} />)

  it('should be properly tested', () => {
    expect(false).toBe(true)
  })
})
