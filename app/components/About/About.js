import React, { PropTypes } from 'react'
import s from './About.scss'

const About = () => (
  <div className={s.root}>
    And routing works too
  </div>
)

About.propTypes = {

}

export default About
